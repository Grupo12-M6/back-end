import { Router } from "express";

const routes = Router();

const adsRoutes = () => {
    // routes.post("/", );
    // routes.patch("/:id",);
    // routes.delete("/:id",);
    // routes.get("/",);
  
    return routes;
}

export default adsRoutes;